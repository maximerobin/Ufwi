#!/bin/sh
# File generated by EdenWall: {{ _generation_time_ }}

. /etc/ha.d/resource.d/hto-mapfuncs

case "$1" in
    start|restart)
        {% if vpn_used %}/etc/init.d/ipsec "$1"{% endif %}
        ;;
    status|monitor)
        if /etc/init.d/ipsec status >/dev/null; then
            log_and_print "INFO: Running OK"
        else
            log_and_print "INFO: ipsec stopped"
        fi
        exit 0
        ;;
    *)
        /etc/init.d/ipsec "$1"
        ;;
esac

