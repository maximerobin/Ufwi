# /etc/pam_ldap.conf
#
# File generated by Edenwall: {{ _generation_time_ }}
#
ldap_version 3
pam_password crypt
scope sub
timelimit 60
bind_timelimit 4
bind_policy soft
base {{ dn_users }}
{% if user %}binddn {{user}}{% else %}#no binddn statements{% endif %}
{% if password %}bindpw {{password}}{% else %}#no bindpw statements{% endif %}
nss_base_passwd {{ dn_users }}
nss_base_group {{ dn_groups }}
uri {{uri}}

